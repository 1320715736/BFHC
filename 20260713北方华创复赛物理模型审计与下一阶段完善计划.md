# 20260713 北方华创复赛物理模型审计与下一阶段完善计划

> 审计日期：2026-07-13  
> 最近更新：2026-07-15（D5 本地技术验证完成）
> 项目目录：`D:\VScode\project\BFHC`  
> Git 快照：`main`，当前已上传提交 `b189c74`（D4）
> 当前阶段：D4 已完成并上传；D5 本地完成待上传，下一阶段 D6
> 本报告用途：替代旧文档中对 A2“外接球已经完成”的判断，作为后续逐项修复和验收的唯一执行清单。

## 1. 总结论

1. **当前代码仍不是最终可提交物理模型。** D1-D4 已闭环外部逃逸辐射、局部退蚀/生命周期、额定规则/温度统计/电极边界、瞬态、材料和升华潜热；D5 已在本地闭环力学后处理与几何筛查；数值收敛仍属于 D6。
2. **D1 已完成并上传。** 两族均以 S2S radiosity × `Famb` 统计 0-3 um 外部逃逸功率，并保留 gross、escape、自吸收与生命周期累计能量；独立 0 K 黑球基准通过。
3. **D2 已完成并上传。** 圆柱侧壁与肩面、zigzag 暴露直段/转角 connector/两端 stub 均映射为守恒体积损失；接触电极外端面仍不辐射、不升华。
4. **生命周期语义已统一。** 20% 失效、寿命 cap、步数上限、低退蚀删失和步间超温均有独立状态；只有成功求解的端点才提交几何、时间和梯形积分能量。
5. **D3 已完成并上传。** 正式额定点按精确全生命周期逃逸能量选取；正式全域 U 无回退；固定室温铜与半无限铜等效边界完成两族 COMSOL 敏感性。
6. **D4 已完成并上传。** 两族真实冷启动、冷热物性角点和阶跃压力测试均不超温；NIST 高温物性及自由表面升华潜热已进入生产热模型。
7. **D5 已完成本地技术验证，但没有给历史候选发放强度通行证。** 相同边界应力对比、双边界包络、高温材料、几何合同和断点续跑已闭环；历史 C10 在 `100 V` 自重压力测试下出现 `5.458 mm` 大位移和局部屈服筛查失败。
8. **zigzag 的 1% 初始边长几何量化是显式数值近似。** 连续退蚀状态和 20% 判据不量化，COMSOL 重建几何量化误差不超过 0.5%；该量化尺度必须在 D6 做收敛验证。
9. **现在仍不应启动贝叶斯搜索。** C1-C10 只作历史回归；D6 通过后再生成新版本 B2/B3 基线和 E1 搜索数据。

## 2. 审计范围和信息优先级

### 2.1 已审计内容

- 赛题与口径：`赛题要求.md`、`温度均匀性评分思路和计算公式.md`、`北方华创 - 问题整理.xlsx`、钉钉群答疑 DOCX、`答辩现场要求.md`、企业命题更新记录及现有工作基线。
- 活跃代码：圆柱簇和 zigzag 簇的 Python runner、Optuna 脚本、分析脚本、Java baseline/process-view 脚本。
- 结果数据：B2/B3、C1-C10 的 CSV、SQLite、日志和候选排名。
- 历史资料：初赛 trials、初赛提交副本、旧 CAD、旧报告/PPT、论文草稿和图片。
- 二进制文件：MPH、STEP、STL 只能通过文件元数据、配套脚本和几何包围盒进行间接审计；最终仍需在 COMSOL 中打开并执行模型树/求解器/选择集检查。

### 2.2 口径优先级

1. 最新企业正式答疑、最新公式文件。
2. 原始赛题要求。
3. 本项目中用户已经明确的执行口径。
4. 旧工作基线和历史报告仅作过程记录；与前三项冲突时不得继续沿用。

### 2.3 当前需保留双口径的事项

| 事项 | 当前处理 | 后续要求 |
|---|---|---|
| 寿命满分档 | `R_L=L_opt/L_ini` 与 `eta_L=(L_opt-L_ini)/L_ini` 同时输出 | 等官方确认满分阈值是 `L_opt/L_ini >= 0.5` 还是 `>=1.5` |
| 有效辐射 `E` | 主字段暂按生命周期累计 0-3 um 逃逸能量 | 同时保留初始功率、生命周期平均功率、累计能量，等待官方最终确认主评分量 |
| 温度均匀性 | 最终优化器件初始额定稳态下，全材料域 `Tmax/Tmin/Tmean` | 该主口径不因自由表面诊断值而替换 |
| 电压 | `100 V` 仅为上限 | 每个几何必须用统一规则确定额定电压 |
| 外接球 | `0 K`、发射率 `1`，只作为有效辐射统计面 | 必须统计真正逃逸到外部的辐射，不能只改局部辐射表达式中的环境温度 |
| 接触端面 | 代码当前不计向外辐射和升华 | 按最新项目口径标记为待官方最终回复；实现应保留可切换选择集 |

## 3. 当前代码和环境快照

### 3.1 运行环境

| 项目 | 当前值 | 风险 |
|---|---|---|
| Python 解释器 | `D:\conda_envs\BFHC\python.exe`，Python 3.11.15 | 已正确切换 |
| `mph` / `JPype1` | 1.3.1 / 1.7.0 | 可连接 COMSOL，但 COMSOL 版本未写入每条结果 |
| Optuna | 4.8.0 | sampler 未统一固定随机种子 |
| NumPy/Pandas/SciPy | 2.4.4 / 3.0.2 / 1.17.1 | 应生成环境锁定文件 |
| `openpyxl` | 未安装 | 不影响仿真；仅影响直接读写 Excel |
| 路径 | 多个脚本仍硬编码 `D:\VScode\project\BFHC` | 迁移和复现实验存在风险 |

静态解析结果：共检查 28 个 Python 文件，24 个活跃文件均能解析；4 个语法错误都位于 `提交文件/初赛提交版本/技术数据/src` 的旧副本。复赛最终提交包不能从该旧目录直接复制，必须由活跃源码重新生成。

### 3.2 当前模型架构

- 圆柱簇：8 个轴向圆柱段参数化半径，COMSOL 稳态 `Electric Currents + Heat Transfer + Surface-to-Surface Radiation`，Python 外循环执行准稳态退蚀。
- Zigzag 簇：Manhattan 方截面 block 与两端圆柱 stub 的 Union，物理接口同上，Python 外循环保存逐块退蚀状态。
- 力学：D5 已建立独立 Python/COMSOL 线性热弹性验证器；不进入优化内层，最终候选应力云图仍待 E2/F2 生成。
- 优化：现有 Optuna 单目标搜索全部冻结；D1-D6 完成前不生成新 trial；D3 额定策略和 D4 热模型版本已写入两个优化入口。

### 3.3 已完成阶段的重新定性

| 阶段 | 已完成内容 | 本次审计后的定性 |
|---|---|---|
| A1-A7 | runner 字段、端面选择、固定室温电极、逐块温度读取、短步验证 | 代码基础有效；原 A2“外接球已完成”结论撤销并转入 D1 重做 |
| B2/B3 | 圆柱官方基线和旧 zigzag 参考重算 | 只属于旧辐射 metric 的控制点，不能作为修模后的正式基线 |
| C1-C5 | 圆柱 20 条验证搜索、80 条扩展搜索及候选分析 | trial 68 保留为回归候选；当前 `eta_E` 排名冻结 |
| C6-C9 | Zigzag 20 条初搜、20 条局部搜索及 trial 19 复核 | trial 19 保留为高遮挡/高寿命压力测试；当前 `eta_E` 排名冻结 |
| C10 | trial 19 提高寿命/步数上限并完成真实失效复核 | 当前物理模型下完成；外接球和局部退蚀修正后必须复算 |
| D1 | 外部逃逸辐射、双波段修正和四个控制点 | 已完成并上传；`radiation_escape_v2` 为后续唯一评分 metric |
| D2 | 两族局部退蚀、精确生命周期状态和体积门禁 | 已完成并上传；`geometry/lifecycle v2` 为后续唯一退蚀口径 |
| D3 | 额定点规则、三域温度统计和电极边界敏感性 | 已完成并上传；提交 `2ae123a` |
| D4 | 冷启动瞬态、高温钨物性和升华潜热反馈 | 已完成并上传；`thermal_s2s_d4_v1` 为后续唯一热模型版本 |
| D5 | 同边界应力对比、几何合同和长 zigzag 压力测试 | 本地技术验证完成，待上传；历史 C10 自重筛查失败，不是正式候选 |

### 3.4 Git 归档规则

- 从 2026-07-13 起，BFHC 项目目录内的源码、数据、日志、文档、图片、COMSOL 模型和历史提交材料全部纳入版本控制，不再使用“仅显示代码文件”的本地忽略策略。
- `.mph`、`.zip`、`.pdf`、`.pptx`、`.docx`、`.xlsx` 由 Git LFS 管理，避免超过 GitHub 普通 Git 的大文件限制。
- 提交和推送仍由用户通过 VS Code 源代码管理手动完成；每个阶段应把对应代码、数据和报告放在同一次阶段提交中。

## 4. 赛题要求逐项审计

判定说明：`已实现` 表示代码和字段均闭环；`部分实现` 表示存在近似或未验证；`未实现` 表示当前模型没有该物理路径；`待确认` 表示官方口径仍需回复。

### 4.1 几何、电学和材料

| ID | 赛题/答疑要求 | 当前代码证据与判断 | 判定 | 必须整改 |
|---|---|---|---|---|
| G01 | 严格从直径 5 mm、高 15 mm 纯钨圆柱开始 | B2 几何为该圆柱；两个 family 都以相同体积为目标 | 已实现 | 修模后重新生成 B2 |
| G02 | 两个电极相对位置严格保持 15 mm | 两个模型均固定 `L0=15 mm`；zigzag 有 0.5 mm 端部 stub | 已实现 | 增加自动端距断言 |
| G03 | 电极为 5 mm 圆形且保持不变，器件只需接触 | 当前用 5 mm 直径端部边界/stub 表示电极接触，未建立真实铜电极域 | 部分实现 | 最终 CAD/报告明确“电极为边界等效”，自动检查连接 |
| G04 | 优化前后钨体积严格相等 | 圆柱容差 `1e-4`；zigzag 容差为 `0.03`，即允许 3% 偏差 | 圆柱已实现；zigzag不合格 | zigzag 改为 COMSOL Union 后体积误差 `<=1e-4`，不以解析体积代替最终检查 |
| G05 | 材料可在原圆柱外重分布 | 两类几何都允许，符合答疑 | 已实现 | 无 |
| G06 | 结构整体、无孤立碎片、可制造 | D5 已检查初始控制点单一域、端距、等体积、最小特征和间隙；实际加工工艺及退蚀后逐步连通性仍待最终候选验证 | D5 部分闭环 | E1/E2 对最终候选及退蚀阶段复核连通性、可夹持性和加工方案 |
| G07 | 若采用体素法，最小空间尺度 0.1 mm | 当前是参数化几何而非体素；C10 方边长约 0.496 mm | 不直接适用 | 仍将 0.1 mm 作为制造/网格最低尺度保护并输出 `minFeature_mm` |
| G08 | 20% 失效特征尺度可由团队定义并解释 | 圆柱用每段半径，zigzag 用每块方边长 | 已实现但需论证 | 报告解释为什么这些尺度代表局部失效；转角和 stub 也需纳入 |
| E01 | 直流通电、欧姆热 | `Electric Currents` 与电磁热耦合已建立 | 已实现 | 增加电功率守恒检查 |
| E02 | 100 V 是电压上限 | runner 有 `voltage_upper=100` | 已实现 | 统一所有基线/候选的额定电压搜索规则 |
| E03 | 电压加在钨两端，不计电极压降、接触电阻 | 两端 Potential/Ground 直接施加于钨端面 | 已实现 | 无 |
| E04 | 每个几何寻找兼顾辐射和寿命的额定工况 | D3 默认和两个优化器均固定 `rated_lifecycle_scan`；两族统一 `1000 h/150` 步，只从精确未删失生命周期中最大化 `lifeTotalP03escape_J` | D3 已实现 | E1 随新 baseline 执行正式扫描；无精确候选必须失败 |
| M01 | 纯钨密度固定，不考虑热膨胀导致的几何变化 | 密度固定 19350 kg/m3；优化热模型不变形 | 已实现 | 力学模型中单独考虑热膨胀，不反馈优化几何 |
| M02 | 电阻率、导热率、比热随温度变化 | D4 两族统一使用 NIST 高温参考模型；保留旧模型和 `rhoe/k/Cp` scale，已实算冷热联合角点 | D4 已实现 | D6 做物性函数/数值容差联合收敛；正式数据记录材料版本和 scale |

### 4.2 导热、辐射和外接球

| ID | 赛题/答疑要求 | 当前代码证据与判断 | 判定 | 必须整改 |
|---|---|---|---|---|
| H01 | 钨内部热传导 | Heat Transfer 已建立并使用温变 `k(T)` | 已实现 | 做能量平衡和网格收敛 |
| H02 | 电极导热边界可选无穷远铜电极或固定室温铜电极 | 固定 `293.15 K` 为主口径；D3 已增加圆形接触半无限铜扩展热阻 Robin 等效敏感性 | D3 已实现 | Robin 仅作包络，不替换主结果；报告声明未显式建立铜无限元域 |
| H03 | 忽略气体对流 | 当前没有对流边界 | 已实现 | 报告写明依据企业答疑 |
| R01 | 0-3 um 发射率 0.35，其余波段 0.15 | D1 发现旧 `lambda_r=lam03` 被 COMSOL 再乘 `um`，第二谱带实际为零；现已改为数值 `3` 并验证全波段功率大于 P03 | D1 已修正 | D6 做谱带/网格收敛 |
| R02 | 0-3 um 是评分辐射，其余波段仍是热耗散 | 热求解包含两波段，评分字段只取 0-3 um | 已实现 | 字段名明确区分 `P03` 与 `Pall` |
| R03 | 考虑结构间相互辐照和自遮挡 | D1 已用 radiosity × `Famb` 统计外部逃逸；四个稳态控制点得到非恒等遮挡损失 | D1 稳态已实现 | 完成生命周期控制点 |
| R04 | 用 0 K、发射率 1 的外接球统计有效辐射 | D1 生产算子为 `rad.J_band1*rad.Famb1`；293.15 K 在评分波段污染 `<5.2e-6%`；独立双灰体封闭腔基准验证黑球吸收与环境逃逸等价 | 等价逃逸法和独立基准已实现 | D6 做数值收敛矩阵 |
| R05 | 外接球只是统计面，不是导热实体 | 未建立导热球体；使用等价环境逃逸后处理 | 已实现 | D6 补球面半径/解析收敛证据 |
| R06 | 接触电极两端面是否辐射、升华 | 当前通过端点发射率置 0、自由表面选择集和退蚀逻辑排除端面 | 已实现且可保留 | 最新项目口径仍记为待官方最终确认；增加开关和选择集测试 |
| R07 | 自遮挡导致净出射功率下降 | 已输出 gross、escape、self-absorbed、raw/clamped loss；trial 68/C10 初始损失约 10.31%/9.16% | D1 稳态已实现 | 生命周期积分完成后闭环 |
| R08 | 辐射环境在热求解中为室温黑体 | S2S 热接口使用 `Tamb=293.15 K`、环境发射率 1 | 已实现 | 与 0 K 评分接口分开，禁止混用物理含义 |

### 4.3 升华、退蚀、寿命和统计字段

| ID | 赛题/答疑要求 | 当前代码证据与判断 | 判定 | 必须整改 |
|---|---|---|---|---|
| S01 | 温度驱动的钨升华/蒸发质量损失 | 两个 runner 使用 `A=3.9e9`、`B=1.023e5` 的指数律并除以密度得到退蚀速率 | 已实现 | 对单位、温度范围和实验数据再做回归测试 |
| S02 | 几何随质量损失变化并重复求解电热辐射 | Python 准稳态外循环每步重建并求解 | 已实现 | 加入版本化历史输出和时间步收敛 |
| S03 | 任意特征尺度变化达到 20% 即失效 | 圆柱逐段半径、zigzag 逐块边长和两端 stub 半径均为独立特征；精确截到 20% | D2 已实现 | D6 验证时间步与 zigzag 几何量化不改变失效时刻 |
| S04 | 圆柱局部表面应按局部温度真实退蚀 | 逐段侧壁温度驱动侧壁损失；内肩面按相邻温度和面积计算，并守恒映射到较大半径段 | D2 已实现（降阶映射） | 报告明确肩面质量损失映射为半径变化；平滑轮廓作为后续候选设计方向 |
| S05 | Zigzag 各局部表面应按局部温度真实退蚀 | 每个 block 保留连续边长状态并独立重建；max-side connector 保证转角连通，暴露直段掩膜排除重叠区 | D2 已实现 | COMSOL 几何采用 1% 初始边长量化，D6 必须做量化尺度收敛 |
| S06 | 所有参与升华的自由表面均应造成质量损失 | 暴露直段、connector 四个外侧面、尺寸差肩面、stub 侧壁和内肩面均进入体积损失；接触外端面排除 | D2 已实现 | 保持端面开关能力，待官方最终确认后只切换选择集规则 |
| S07 | 升华潜热对热平衡的反作用 | D4 使用与退蚀相同的质量通量乘 `4.62 MJ/kg` 作为自由表面冷却；接触端面排除；控制点影响 `<0.05 K` | D4 已实现并量化 | 默认保持启用；D6 纳入能量守恒，E1 最终候选重新量化占比 |
| L01 | 初始和整个生命周期均不得超过 3273.15 K | 初始检查和每个退蚀步 `maxErosionTmax_K`/`FAIL_OVERTEMP_DURING_EROSION` 已实现 | 已实现 | 增加步间裕量和失败历史保存 |
| L02 | 生命周期平均 0-3 um 功率 | D1 escape 功率按成功端点做梯形积分，除以已提交寿命 | D1-D2 已实现 | D6 做时间步收敛 |
| L03 | 生命周期累计有效辐射能量 | `lifeTotalP03escape_J` 为主字段，`lifeTotalP03sphere_J` 为兼容别名；初始/平均/累计量同时保留 | D1-D2 已实现 | 正式报告不得混用 gross 与 escape |
| L04 | 仿真上限不应冒充真实寿命 | 两族统一输出 `failureReached/capLimited/stepLimited/censored/lifetimeExact/terminationReason` | D2 已实现 | 只有 `failureReached=true` 且 `status=OK` 才可报告等号寿命 |
| L05 | 时间积分应覆盖最后一个有效区间 | 两族均按“候选推进-端点求解-超温插值-积分-状态提交”执行；失败端点不提交 | D2 已实现 | D6 对最大时间步做寿命和能量收敛 |
| T01 | 最终优化器件稳态 `Tmax/Tmin/Tmean/U` | D3 全材料域体积最大、最小、平均和 U 直接读取 COMSOL；所有正式回退已删除 | D3 已实现 | 任一算子失败即整次求解失败 |
| T02 | 温度均匀性 `U=(Tmax-Tmin)/Tmean*100%` | 正式全域 U 保留；D3 新增活动区和自由表面诊断，固定端 smoke 圆柱/zigzag U 约 141%/98% | 已实现但当前性能不合格 | 诊断值不得替代主指标；E1 正面优化并继续确认官方统计域 |
| T03 | 数值温度满足边界物理 | D3 固定端 smoke 最低温下穿仅 `5.7e-5 K`；超过 `1 K` 会硬失败 | D3 已核查 | D6 继续做网格/容差收敛 |

### 4.4 瞬态、应力、优化系统和交付物

| ID | 赛题/答疑要求 | 当前代码证据与判断 | 判定 | 必须整改 |
|---|---|---|---|---|
| X01 | 稳态温度低于 3000 摄氏度 | 已计算和约束 | 已实现 | 修模后重验 |
| X02 | 瞬态温度也低于 3000 摄氏度 | D4 两族 Python runner 已增加真实 Time Dependent 冷启动；0.5 s 线性升压、冷热物性角点和瞬时阶跃共 8 个控制工况均通过 | D4 已实现 | 新 baseline、Pareto 候选和最终候选必须重跑；D6 验证输出步长和容差 |
| X03 | 初始与优化结构应力场对比 | D5 已用高温材料完成控制点相同边界对比、双端固定/一端滑移包络和可选屈曲/重力筛查 | D5 接口已实现 | E2 对新初始基线与最终候选导出同边界应力/位移云图和曲线 |
| X04 | 初始起伏 200 um、周期 500 um | 选做项，当前 runner 没有 | 未实现（选做） | 主模型闭环后做粗糙度寿命敏感性，可形成加分项 |
| A01 | 可用其他机器学习方法，不强制 RL | 当前 Optuna/BO 合规 | 已实现 | 报告准确表述为贝叶斯优化，不冒充强化学习 |
| A02 | 算法要高效、迭代不超过 500 次 | 当前复赛搜索约 140 个主要 trial，但修模后旧 trial 不可混入 | 部分实现 | 新物理版本设总预算，建议 260-320 次并完整记录 |
| A03 | 方法创新与可解释性 | 现有 TPE 与旧 SHAP 脚本较常规，且 SHAP 默认读取初赛旧数据 | 部分实现 | 新数据训练代理模型，做 Pareto、SHAP/PDP、设计规则和不确定性分析 |
| O01 | 最终 STEP 和 STL | 现有 CAD 是 5 月初赛旧候选，不是 trial 68/C10，更不是修模后的最终结构 | 未实现 | F1 从最终参数自动导出并做几何校验 |
| O02 | 初始到最终拓扑演化动画 | 当前仅有搜索日志/静态图，没有当前版本自动动画 | 未实现 | 保存每次有效 trial 和退蚀阶段几何，生成演化视频 |
| O03 | 温度、应力和均匀性变化曲线 | 温度/U CSV 有基础；最终应力和动画曲线未闭环 | 部分实现 | F2 统一生成可追溯图表和答辩视频素材 |
| O04 | 最终仿真模型和技术数据可复现 | runner 可自动建模、支持部分断点恢复并写入 metric/physics/material/geometry/lifecycle 版本；仍有路径硬编码、无环境锁及缺少 Git/COMSOL 版本的问题 | 部分实现 | 相对路径、随机种子、环境文件、Git/COMSOL 版本写入每条结果 |

## 5. 外接球问题的正确修复定义

### 5.1 当前错误不是“没有画一个球”这么简单

赛题中的外接球是三维**外接球统计面**，不是外接圆，也不是需要参与导热的实体。当前局部表达式

```text
eps03 * sigma * f03(T) * T^4
```

只能得到每个自由表面的 0 K 本征发射量。凹面、相对表面和密集 zigzag 中，一部分辐射会打到自身其他表面并被吸收或反射；只有最终逃逸到外部黑体环境的部分可以记为 `P03sphere`。

COMSOL 的 S2S 接口本身使用 radiosity 和 view factor，环境视角因子表示未被其他辐射面覆盖、能够逃逸到环境的比例。可依据官方 [Surface-to-Surface Radiation 接口](https://doc.comsol.com/6.3/doc/com.comsol.help.heat/heat_ug_interfaces.08.49.html)、[Radiosity Method](https://doc.comsol.com/6.3/doc/com.comsol.help.heat/heat_ug_theory.07.051.html) 和 [View Factor Evaluation](https://doc.comsol.com/6.3/doc/com.comsol.help.heat/heat_ug_theory.07.053.html) 实现；官方[同心球 View Factor 示例](https://doc.comsol.com/6.4/doc/com.comsol.help.models.heat.view_factor/view_factor.html)可作为回归基准。

### 5.2 推荐实现

1. 保留现有 `radThermal`：环境温度 293.15 K，参与稳态/瞬态热平衡。
2. 新建独立 `radScore` 或独立后处理研究：导入已经求得的钨温度场，环境/接收球设为 0 K、发射率 1，不反向改变热解。
3. 生产统计可用 S2S 环境逃逸 radiosity 与 `Famb` 积分；同时建立一个显式黑色球面接收验证模型，证明两种方法等价。
4. 分别输出：

```text
P03gross_W                 自由表面 0-3 um 本征发射总量
P03escape_W                最终逃逸到 0 K 黑色外部环境的净功率
P03selfAbsorbed_W          P03gross_W - P03escape_W
selfViewLoss_pct           P03selfAbsorbed_W / P03gross_W * 100%
lifeAvgP03escape_W         生命周期平均逃逸功率
lifeTotalP03escape_J       生命周期累计逃逸能量
```

5. 原字段 `P03sphere` 只可作为 `P03escape` 的兼容别名，必须同时写入 `metricVersion`，防止与旧 CSV 混用。

### 5.3 D1 验收测试

- 凸均匀圆柱的遮挡损失接近 0，且绝不因恒等赋值被强制为 0。
- 两个相向平板/折返结构的 `P03escape < P03gross`，增加遮挡后损失单调增大。
- 显式球面接收法与环境逃逸法差异 `<1%`。
- 球半径取包围半径的 1.05、1.5、2.0 倍时，接收总功率变化 `<0.5%`。
- 全波段能量平衡满足 `P_electric = P_end_conduction + P_radiation + P_sublimation`；未耦合潜热时应单列该项，主残差目标 `<1%`、最低接受 `<2%`。
- 任何正式结果若 radiosity 变量、选择集或球面积分读取失败，必须返回失败状态，禁止回退为局部发射量。

## 6. 当前结果的有效范围

### 6.1 当前关键结果快照

下表中的 `E` 全部是旧 metric 的自由表面积分累计量，仅用于说明当前推进位置，不代表外接球评分结果。

| 案例 | 寿命 h | 旧 `E` MJ | U % | 当前定位 |
|---|---:|---:|---:|---|
| B2 官方圆柱 | 242.0791 | 105.598 | 148.693 | 修模后的 baseline 重算输入 |
| B3 旧 zigzag | 11.3271 | 114.728 | 98.393 | 参考失败案例 |
| 圆柱 trial 68 | 378.3357 | 243.679 | 127.037 | 圆柱控制候选 |
| Zigzag C10 trial 19 | 647.0447 | 4440.513 | 97.098 | 高遮挡、高寿命控制候选 |

### 6.2 C10 历史结果

| 指标 | 历史值 | D1-D2 后的定位 |
|---|---:|---|
| `N_RUNS` / `L_RUN` / `z_first` | 12 / 92.011 mm / 2.267 mm | 仅作待复核候选参数 |
| 横截面边长 | 0.4958 mm | 初始解析值可保留，需严格 COMSOL 体积复核 |
| 路径总长 | 1118.134 mm | 可保留，但 92 mm 横向尺度有制造/结构风险 |
| 额定电压 | 100 V | 只是当前 `max_safe` 规则结果，需做全电压曲线 |
| `lifetimeH` | 647.0447 h | 旧辐射/退蚀模型下的条件性寿命，不再有效 |
| `failureReached` | True | 只说明该历史运行不是时间上限截断 |
| `lifeTotalP03sphere_J` | 4.4405e9 J | **历史值无效；D1 已改为外部 escape，D2 已改退蚀几何** |
| `selfViewLoss_pct` | 0 | **历史值无物理意义；D1 已输出真实非零遮挡损失** |
| `U_pct` | 97.0981% | 公式口径可参考，但 `<10%` 评分档不合格，且需核查 Tmin |
| `maxErosionTmax_K` | 2868.882 K | 旧模型下通过；D4-D6 后随正式基线重验 |

### 6.3 数据分级

| 数据/产物 | 当前用途 | 禁止用途 |
|---|---|---|
| B2 圆柱基线 | D1-D6 回归案例 | 最终 `E_ini` 和最终排名 |
| B3 旧 zigzag 参考 | 失败模式回归 | 官方 baseline 或最终候选 |
| 圆柱 C4/C5 trial 68 | 外接球与退蚀修复后的对照候选 | 当前 `eta_E` 直接写入报告 |
| Zigzag C8/C10 trial 19 | 高遮挡、高寿命压力测试 | 当前 4105% 能量提升作为正式结论 |
| 初赛两族各 150 条 trials | 仅可做代码/参数范围历史参考 | 任何复赛定量训练、代理模型、排名和图表 |
| 当前复赛 CSV/DB | 回归输入，必须标记 `legacyMetric` | 与修复后数据拼表训练 |
| 旧 MPH/STEP/STL/PPT/论文图 | 版式和流程参考 | 冒充当前最终模型或当前性能 |

## 7. 还需修正的代码级问题

### 7.1 两个 runner 共性问题

- D3 已删除正式 `Tmin=0.95*Tmax` 回退，并以 `temperatureFallbackUsed=false` 和算子硬失败锁定。
- 结果已有 `physicsVersion/metricVersion/geometryVersion/lifecycleVersion`，但仍缺 `COMSOLVersion/gitCommit/randomSeed/meshLevel`。
- 没有正式的网格、退蚀时间步和电压搜索收敛报告。
- D2 已统一退蚀端点事务、失败状态和重试次数；进程级 COMSOL 断连仍需由优化器断点机制重启同一点。
- 没有自动导出每个退蚀阶段的质量、几何、温度、功率和能量守恒历史。
- 热求解包含 3 um 外辐射，这是正确的；但字段命名应防止把“总热辐射”与“0-3 um 评分辐射”混淆。

### 7.2 圆柱簇特有问题

- 8 段阶梯圆柱并非光滑、连续、容易制造的最终轮廓。
- 肩面升华质量已守恒映射到较大段半径，但不是显式轴向法向移动；最终报告必须说明该降阶假设。
- 步数上限已返回 `CENSORED_STEP_LIMIT`，不再冒充真实 20% 寿命。
- 旧 trial 68 排名已经冻结；D4-D6 后必须按新版本重算。

### 7.3 Zigzag 簇特有问题

- 体积门槛已统一为 `1e-4`，并区分 COMSOL-当前几何误差与当前几何-官方初始体积偏差。
- 逐块连续退蚀状态已落实为局部几何；为避免微米级非流形转角，COMSOL 几何按初始边长的 1% 量化，D6 必须验证收敛。
- 端部 stub 侧壁/内肩面、转角 connector 外侧面和尺寸差肩面均已进入退蚀；接触外端面排除。
- C10 的单条横向 run 约 92 mm、总路径 1.118 m；题目未给硬包络限制，但机械稳定性、热变形、重力、加工和安装合理性不足。
- cap、step、低退蚀和失败求解均已使用显式删失状态；最后有效区间已纳入积分。
- C10 的旧 `Tmin` 下穿约 1 K 已由 D3 新算子和硬门禁处理；旧值不再用于正式结论。

### 7.4 优化和分析脚本问题

- D3 已将正式优化器固定为 `rated_lifecycle_scan`；历史复核脚本显式保留 `max_safe`，两类数据禁止混用。
- 单目标函数在 U 优于旧基线后不再奖励继续降低 U，无法主动冲击官方 `<10%` 档。
- 对寿命、能量和均匀性使用人为加权，缺少 Pareto 前沿和约束敏感性。
- Optuna sampler 没有显式固定 seed；resume 代码部分依赖私有存储 API。
- 多个路径硬编码；旧 `parse_results.py` 和 `surrogate_analysis.py` 默认读取初赛 `trials.csv` 和旧 baseline。
- 新物理版本重算前，任何 SHAP/代理模型结论都会学习错误的辐射和退蚀目标。

### 7.5 Java/COMSOL 展示模型问题

- `cylinder_process_views.java` 仍是均匀 2.5 mm 圆柱，不是当前圆柱候选。
- `zigzag_process_views.java` 仍是 N=8、L=104 mm 的旧结构，不是 C10。
- 力学使用常数 `E=411 GPa`、`nu=0.28`、`alpha=4.5e-6/K`，且两端完全固定；高温材料软化、轴向滑移和蠕变未评估。
- 四个稳态阶段只是数据集切换，不等于真实瞬态研究，也没有自动保存最终 MPH、STEP、STL 和动画。

## 8. 下一阶段逐项完善计划

执行规则：每一阶段通过验收并生成独立结果后再勾选。D1-D6 完成前禁止启动新的大规模 BO。C10 之后不再追加 C 编号。

| 勾选 | 阶段 | 工作内容 | 验收标准 | 主要产物 |
|---|---|---|---|---|
| [x] | C10 | C8-local trial 19 提高到 1000 h/150 步复核 | 已测到真实 20% 失效，非 cap/step 截断 | 当前模型条件性候选 |
| [x] | D1 | 外接球评分闭环，并复算四个控制点 | 已完成并上传：10/10 回归、四个稳态/生命周期点和独立黑球基准通过 | 两族 metric v2 runner、控制点 CSV、D1 报告 |
| [x] | D2 | 两族退蚀几何和生命周期闭环 | 已完成并上传：32/32 回归；两族 geometry/lifecycle COMSOL smoke 通过 | geometry/lifecycle v2、D2 报告、JSON/日志 |
| [x] | D3 | 统一额定电压、温度统计和电极热边界 | 已完成并上传；提交 `2ae123a` | `D3额定工况与温度统计闭环验证报告.md` |
| [x] | D4 | 瞬态、材料不确定性和升华潜热评估 | 已完成并上传：提交 `b189c74`；58/58 回归；8 个瞬态、6 个稳态敏感性工况通过 | `D4瞬态材料与升华潜热闭环验证报告.md` |
| [ ] | D5 | 应力、位移、结构稳定性和可制造性验证 | 本地技术验证完成，待上传；历史 C10 自重压力测试暴露大位移/屈服风险 | `D5力学稳定性与可制造性闭环验证报告.md` |
| [ ] | D6 | 网格、时间步、球半径和能量守恒收敛 | 功率差<1%、Tmax差<5 K、寿命差<2%、能量残差<2% | verification matrix |
| [ ] | E1 | 重算 B2/B3，并完成两族新物理版本搜索 | 数据带完整版本字段；总预算建议 260-320 次且<500；输出 Pareto 前沿 | 新 baseline、trials、DB 和候选排名 |
| [ ] | E2 | 最终候选稳健性与高分增强 | 参数扰动、粗糙度和重复求解后仍满足约束；形成解释性设计规则 | 稳健候选与加分项报告 |
| [ ] | F1 | 最终模型和技术数据归档 | MPH 可独立求解；STEP/STL 体积/端距/连通性一致；数据可追溯 | 最终模型包 |
| [ ] | F2 | 技术报告、PPT、5 分钟视频 | 包含拓扑演化、温度/U/应力曲线、多角度结构、评分逐项对照 | 答辩材料 |
| [ ] | F3 | 提交前一致性检查 | 团队编号、文件名、代码语法、许可证、模型版本、数据和图表完全一致 | release checklist |

## 9. 每阶段统一验收门槛

后续每修复一项，都必须同时满足以下门槛，不能只以“COMSOL 能收敛”判完成。

| 类别 | 门槛 |
|---|---|
| 几何 | 端距 15 mm；钨初始体积误差 `<=1e-4`；单一连通域；无非法重叠/孤立域 |
| 电压 | `0<V<=100 V`；额定策略和 baseline 完全一致 |
| 温度 | 初始及全部退蚀/瞬态阶段 `Tmax<3273.15 K`；统计无回退 |
| 辐射 | 0-3 um 与全波段分开；`0<=Pescape<=Pgross`；外接球方法通过半径/解析回归 |
| 寿命 | 明确 `failureReached/capLimited/stepLimited/censored`；只有真实 20% 失效可报等号寿命 |
| 能量 | 电功率、端部导热、全波段辐射及潜热闭合，残差 `<2%` |
| 数值 | 网格/时间步收敛满足 D6；记录实际 mesh level、自由度和求解容差 |
| 可复现 | 每行记录 Git commit、COMSOL/Python/package、seed、physics/metric/geometry version |
| 数据 | 旧物理版本只读归档；不同版本不得合并训练或直接比较 `eta_E` |
| 输出 | CSV、DB、MPH、STEP/STL、图片的 case ID 和参数一一对应 |

## 10. 面向高分的改进方向

### 10.1 先拿稳物理和系统分

- 用外接球逃逸、能量守恒、收敛矩阵和材料敏感性证明结果可信，这比继续堆 trial 更能提高“仿真系统”和“物理洞察”分。
- 把每个候选的输入参数、几何、物理版本、失败状态、全过程历史自动归档，答辩时可从任一 CSV 行复现 MPH。
- 对圆柱 trial 68 与 zigzag C10 做“收益来源分解”：面积、温度、0-3 um 光谱占比、遮挡、端部导热、退蚀热点分别贡献多少。

### 10.2 将优化改为受约束多目标

- 主目标使用 `lifeTotalP03escape_J`，同时把寿命、初始功率、U、结构尺度和应力作为独立目标/约束，不再只靠单一手工加权分数。
- 先用低成本解析/代理模型筛选，再由 COMSOL 高保真复核，控制总迭代数并体现轻量化方法。
- 新搜索采用固定 seed、重复种子和 Pareto 前沿；最终选“稳健解”，不只选一次运行的最大值。

### 10.3 避免利用模型漏洞得到虚高方案

- 不允许存在只增加辐射面积却不参与退蚀的肩面、端面或 stub。
- Zigzag 虽可超出原圆柱包络，但应加入软包络、最小间距、最小弯曲半径、最大长细比和结构稳定性约束。
- 对 C10 这类超长路径至少比较：无重力/有重力、两端固定/一端滑移、室温/高温材料参数，避免最终方案只能在理想数学模型中成立。

### 10.4 争取创新和解释分

- 在物理闭环后，用代理模型的 SHAP/PDP 和参数扫描提炼“何处增材、何处减材、为何降低热点或遮挡”的规则。
- 可增加小规模 model-based RL 或主动学习作为外层选点器，但不应在物理模型修正前投入大量算力。
- 完成选做的表面起伏鲁棒性：同一候选在无起伏和 200 um/500 um 正弦起伏下比较寿命和热点迁移。

## 11. 建议向官方集中确认的问题

这些问题不阻塞 D1-D6，但会影响最终表格表述：

1. 寿命满分档是 `L_opt/L_ini >= 0.5`，还是 `eta_L>=50%` 即 `L_opt/L_ini>=1.5`？
2. `E` 的正式主评分量是否明确为生命周期累计 0-3 um 外接球净辐射能量？初始功率和平均功率是否只作辅助指标？
3. 接触电极的两个钨端面“不向外辐射、不升华”是否为复赛最终不变口径？
4. 全域 U 是否包含施加固定 293.15 K 边界的接触端面？若包含，在高温额定工况下 `U<10%` 与固定冷端存在结构性矛盾。
5. Excel 中“初始圆柱额定电压不到 0.3 mV”是否应为约 0.3 V？钉钉手写推导约为 0.34 V，当前 B2 为 1.168 V，需确认参考边界条件。

## 12. 下一条实际执行任务

D4 已完成并上传。D5 应力、位移、结构稳定性和可制造性验证已在本地闭环，待 Git 上传后勾选归档；上传后执行 **D6：网格、时间步、外接球统计和能量守恒收敛**。D6 通过前不重算正式 baseline，不启动 BO。
